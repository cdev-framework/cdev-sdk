from core.constructs.commands import BaseCommandContainer


class SimpleCommandContainer(BaseCommandContainer):
    help = """
These are the commands that are available for use on a terminal to explore files.

shell: Open an interactive terminal to your zip file.
"""
